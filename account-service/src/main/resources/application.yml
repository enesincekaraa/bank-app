spring:
  config:
    import: "configserver:"
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"

